# Postgres connection string
DATABASE_URL=postgresql://user:password@localhost:5432/som_erp

PORT=3001

# "production" in any real deployment — several security guards (the
# BYPASS_AUTH dev-auth-skip below, the CORS whitelist branch in server.js)
# key off this being exactly "production"; leaving it unset behaves like dev.
NODE_ENV=development

# HMAC signing secret for auth JWTs — required, no fallback. Generate with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Rotating this value invalidates every currently-issued token (forces
# re-login everywhere) — both frontends already handle this automatically
# via their existing 401 interceptors.
JWT_SECRET=

# Dev-only convenience — skips authentication entirely and injects a
# hardcoded super-admin user. Ignored unless NODE_ENV !== production, but
# never set this to true anywhere near a real deployment.
BYPASS_AUTH=false

# Comma-separated list of allowed frontend origins, checked in production
# mode only (dev mode allows any http://localhost:<port> automatically).
# This app has two frontends — main ERP and the admin panel.
FRONTEND_URLS=https://erp.example.com,https://admin.example.com

# Optional defense-in-depth for the first-run admin setup page (/setup —
# see backend/src/modules/user/setup/). That page already self-disables the
# instant any user exists in the database, but on a freshly deployed,
# publicly reachable server there's a brief window where anyone who finds
# the URL first could create the first (Super Admin) account instead of you.
# Set this to a random value BEFORE deploying, then enter it once on the
# setup page — leave unset to skip this extra check entirely (fine for
# local dev, or if you complete setup immediately after deploy and before
# the server is publicly reachable).
SETUP_SECRET=

# WhatsApp/SMS notifications via MSG91 (backend/src/services/whatsapp-service.js).
# Primary channel is WhatsApp Business API; falls back to SMS if WhatsApp fails.
# Set WHATSAPP_ENABLED=true to actually send — otherwise sends are mocked/logged only.
WHATSAPP_ENABLED=false
# Your MSG91 auth key (from the MSG91 dashboard).
MSG91_AUTH_KEY=
# SMS sender ID, e.g. SOMERP (6 chars, DLT registered).
MSG91_SENDER_ID=
# Your MSG91 integrated WhatsApp number, e.g. 919876543210.
MSG91_WHATSAPP_NUMBER=
# Approved WhatsApp template name in MSG91 (optional — if unset, sends as a
# freeform session message instead of a business-initiated template).
MSG91_TEMPLATE_NAME=
# Template language code, default: en
MSG91_TEMPLATE_LANG=
