model ProductionPlan {
  id               String   @id @default(cuid())
  planId           String   @unique @map("plan_id")
  salesOrderId     String?  @map("sales_order_id")
  salesOrderItemId String?  @map("sales_order_item_id")
  diNo             String   @map("di_no")
  customerName     String?  @map("customer_name")
  sectionType      String   @map("section_type")
  plannedDate      DateTime @map("planned_date")
  shift            String?
  batchIncharge    String?  @map("batch_incharge")
  noOfCycles       Int?     @map("no_of_cycles")
  cycleBatchSize   Float?   @map("cycle_batch_size")
  equipment        String?
  location         String?
  productCode      String?  @map("product_code")
  productName      String   @map("product_name")
  batchCode        String?  @map("batch_code")
  totalQty         Float    @map("total_qty")
  uom              String   @default("kg")
  carrier          String?
  specs            String?
  process          String?
  stageInfo        String?  @map("stage_info")

  unitPackQty      Float?  @map("unit_pack_qty")
  noOfUnits        Int?    @map("no_of_units")
  pp1              String?
  pp2              String?
  sp1              String?
  noOfSp           Int?    @map("no_of_sp")
  labels           String?
  packingSpec      String? @map("packing_spec")
  perDayCompletion Int?    @map("per_day_completion")
  femaleWorkers    Int?    @map("female_workers")
  maleWorkers      Int?    @map("male_workers")

  status         String   @default("DRAFT")
  rmCheckStatus  String?  @map("rm_check_status")
  eqpCheckStatus String?  @map("eqp_check_status")
  rmCheckDetails Json?    @map("rm_check_details")
  autoGenerated  Boolean  @default(false) @map("auto_generated")
  remarks        String?
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")
  createdBy      String?  @map("created_by") @db.VarChar(255)
  updatedBy      String?  @map("updated_by") @db.VarChar(255)

  @@map("production_plan")
}

model PlanSequence {
  year Int @id
  seq  Int @default(0)

  @@map("plan_sequence")
}

model PlannerLog {
  id              String   @id @default(cuid())
  runAt           DateTime @default(now()) @map("run_at")
  trigger         String
  plansCreated    Int      @default(0) @map("plans_created")
  ordersProcessed Int      @default(0) @map("orders_processed")
  errors          String?

  @@map("planner_log")
}

model BomSend {
  id          String    @id @default(cuid())
  sendId      String    @unique @map("send_id")
  indentId    String?   @map("indent_id")
  planId      String    @map("plan_id")
  productCode String    @map("product_code")
  productName String    @map("product_name")
  batchNo     String    @map("batch_no")
  diNo        String    @map("di_no")
  sectionType String?   @map("section_type")
  bomType     String    @map("bom_type")
  totalQty    Float     @map("total_qty")
  uom         String    @default("kg")
  status      String    @default("PENDING")
  remarks     String?
  sentBy      String?   @map("sent_by")
  sentAt      DateTime  @default(now()) @map("sent_at")
  pickedAt    DateTime? @map("picked_at")
  issuedAt    DateTime? @map("issued_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")
  createdBy   String?   @map("created_by") @db.VarChar(255)
  updatedBy   String?   @map("updated_by") @db.VarChar(255)

  @@map("bom_send")
}

model SheetSyncLog {
  id        String   @id @default(cuid())
  trigger   String   @default("WEBHOOK")
  rowsIn    Int      @default(0) @map("rows_in")
  imported  Int      @default(0)
  skipped   Int      @default(0)
  errors    Json?
  createdAt DateTime @default(now()) @map("created_at")

  @@map("sheet_sync_log")
}
